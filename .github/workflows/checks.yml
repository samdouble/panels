name: Checks
on: [pull_request]

permissions:
  contents: read
  pull-requests: read

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Commitlint
        uses: wagoid/commitlint-github-action@v6
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '7.0'
      - name: Audit
        run: |
          dotnet restore
          dotnet list package --vulnerable --include-transitive | tee vulnerable.out
        # test `grep -cm 1 'has the following vulnerable packages' vulnerable.out` = 0
      - name: Test
        run: |
          dotnet test
      - name: Build
        run: |
          dotnet Build
      - name: Release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          dotnet tool install --global dotnet-releaser --version 0.9.0
          dotnet-releaser build dotnet-releaser.toml
          dotnet-releaser publish --force --github-token $GITHUB_TOKEN dotnet-releaser.toml
